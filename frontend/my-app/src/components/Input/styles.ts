import { Dimensions, StyleSheet } from "react-native";

export const style = StyleSheet.create({
    input:{
        width: '100%',
        height: 40,
        borderWidth: 0.6,
        borderRadius: 8,
        borderColor: 'gray'
    }

})